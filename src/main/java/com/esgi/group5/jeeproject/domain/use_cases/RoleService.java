package com.esgi.group5.jeeproject.domain.use_cases;
/*
import com.esgi.group5.jeeproject.persistence.datatbase.daos.RoleDAO;
import com.esgi.group5.jeeproject.repositories.contracts.IRoleRepository;
import org.springframework.stereotype.Service;

@Service
public class RoleService implements IRoleService {
    private IRoleRepository roleRepository;

    public RoleService(IRoleRepository roleRepository) {
        this.roleRepository = roleRepository;
    }

    private RoleDAO get(String label) {
        return roleRepository.get(label);
    }

    private RoleDAO add(String label) {
        return roleRepository.add(label);
    }

    @Override
    public RoleDAO getAdminRole() {
        return get("ROLE_ADMIN");
    }

    @Override
    public RoleDAO getUserRole() {
        return get("ROLE_USER");
    }
}
*/